//
//  StaticValues.swift
//  Harmony Leap
//
//  Created by Den on 04/04/24.
//

import Foundation

enum StaticValues {
    enum logo: String {
        case logo = "splashLogo"
    }
    enum onboarding: String {
        case slide0 = "slide 0"
        case slide1 = "slide 1"
    }
    enum elements: String {
        case graph = "graph"
        case abstract = "element"
    }
    enum show: String {
        case onboarding = "onboarding"
    }
}
